{
  "watch": ["src"],
  "ext": "ts,js",
  "ignore": ["src/**/*.spec.ts", "node_modules"],
  "exec": "ts-node --files",
  "env": {
    "NODE_ENV": "development",
    "NODE_OPTIONS": "--max-old-space-size=512"
  }
}
